This program uses methods to do three different tasks. It converts a Fahrenheit temperature to Celsius,
finds the hypotenuse of a triangle, and rolls two dice to get their total.

The pogram has only one file, the Main class that runs the code and stores the methods.
The methods are the ones who return the values back to the main and then print the results
